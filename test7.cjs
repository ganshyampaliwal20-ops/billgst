require('dotenv').config({path: '.env.local'}); const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); const now = new Date(); const expiryDate = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000); pool.query('UPDATE users SET plan_expiry = $1 WHERE id = (SELECT id FROM users LIMIT 1)', [expiryDate]).then(r => console.log('success')).catch(e => console.log('DB ERROR:', e.message)).finally(()=>process.exit(0));
