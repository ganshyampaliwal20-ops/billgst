require('dotenv').config({path: '.env.local'}); const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); pool.query('SELECT has_claimed_free_plan FROM users WHERE id = $1', ['123456789']).then(r => console.log('success')).catch(e => console.log('DB ERROR:', e.message)).finally(()=>process.exit(0));
