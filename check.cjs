process.env.NODE_TLS_REJECT_UNAUTHORIZED = " 0\; const { Client } = require('pg'); require('dotenv').config({ path: '.env.local' }); const client = new Client({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); client.connect().then(() => client.query(\SELECT column_name FROM information_schema.columns WHERE table_name =  invoices\)).then(res => console.log('Columns:', res.rows.map(r => r.column_name).join(', '))).catch(console.error).finally(() => client.end());
