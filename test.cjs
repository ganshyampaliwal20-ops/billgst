require('dotenv').config({path: '.env.local'}); const { Pool } = require('pg'); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); pool.query('BEGIN').then(() => pool.query("UPDATE users SET has_claimed_free_plan = true, plan_type = 'PREMIUM_99', plan_expiry = NOW() + INTERVAL '30 days', subscription_status = 'ACTIVE' WHERE id = (SELECT id FROM users LIMIT 1)")).then(r => pool.query('ROLLBACK')).then(() => { console.log('SUCCESS'); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); })
